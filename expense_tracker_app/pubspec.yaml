name: expense_tracker_app
description: A Flutter app for tracking expenses
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.1
  shared_preferences: ^2.0.8
  intl: ^0.17.0
  fluttertoast: ^8.2.6  # Updated version constraint

dev_dependencies:
  flutter_test:
    sdk: flutter
